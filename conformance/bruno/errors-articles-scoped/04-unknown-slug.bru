meta {
  name: Get unknown article
  type: http
  seq: 4
}

get {
  url: {{host}}/api/articles/unknown-scoped-{{uid}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
}

script:post-response {
  expect(res.body.errors.article[0]).to.eql("not found");
}
