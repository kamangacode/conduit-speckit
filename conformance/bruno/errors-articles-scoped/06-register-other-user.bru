meta {
  name: Register other user
  type: http
  seq: 6
}

post {
  url: {{host}}/api/users
  body: json
  auth: none
}

body:json {
  {
    "user": {
      "username": "scoped_other_{{uid}}",
      "email": "scoped_other_{{uid}}@test.com",
      "password": "password123"
    }
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("token_other", res.body.user.token);
}
