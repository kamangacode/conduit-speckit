meta {
  name: Register owner
  type: http
  seq: 2
}

post {
  url: {{host}}/api/users
  body: json
  auth: none
}

body:json {
  {
    "user": {
      "username": "scoped_owner_{{uid}}",
      "email": "scoped_owner_{{uid}}@test.com",
      "password": "password123"
    }
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("token_owner", res.body.user.token);
}
