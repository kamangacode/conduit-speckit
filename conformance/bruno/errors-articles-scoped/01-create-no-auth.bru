meta {
  name: Create article without auth
  type: http
  seq: 1
}

post {
  url: {{host}}/api/articles
  body: json
  auth: none
}

body:json {
  {
    "article": {
      "title": "Scoped no auth {{uid}}",
      "description": "test",
      "body": "test"
    }
  }
}

assert {
  res.status: eq 401
}

script:post-response {
  expect(res.body.errors.token[0]).to.eql("is missing");
}
