spring:
  application:
    name: conduit
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME:}
    password: ${DATABASE_PASSWORD:}
  flyway:
    enabled: true
conduit:
  jwt:
    secret: ${JWT_SECRET}
