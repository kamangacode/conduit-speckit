spring:
  datasource:
    url: jdbc:h2:mem:conduit;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
conduit:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
