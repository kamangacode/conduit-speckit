DATABASE_URL: jdbc:h2:mem:conduit;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
DATABASE_USERNAME: sa
DATABASE_PASSWORD: test-only

spring:
  datasource:
    url: jdbc:h2:mem:conduit;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
conduit:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
